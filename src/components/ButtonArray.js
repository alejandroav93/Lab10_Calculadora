/* eslint-disable react/prop-types */
import React from 'react';
import './ButtonArray.css';

function ButtonArray({ children }) {
  return <div className="buttonArray">{children}</div>;
}

export default ButtonArray;
